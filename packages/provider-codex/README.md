# OfferU Codex Provider

Provider adapter for Codex CLI.

Current state:

- Checks whether `codex` is installed.
- Exposes provider metadata and capabilities.
- Provides a session interface placeholder for the daemon.

Execution will be added after the daemon approval model is wired.
