# OfferU Claude Provider

Provider adapter for Claude Code CLI.

Current state:

- Checks whether `claude` is installed.
- Exposes provider capabilities.
- Implements the common provider interface with a non-executing session placeholder.
