[project]
name = "ucareer-py-api"
version = "0.1.0"
description = "Python cloud API skeleton for Ucareer"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115",
  "uvicorn>=0.30",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
