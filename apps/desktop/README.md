# Ucareer Desktop

Desktop shell placeholder.

Target stack:

- Tauri + TypeScript first
- Electron fallback if PTY embedding or process control requires it

Responsibilities:

- Start and supervise the local daemon
- Pair with the cloud API
- Keep source repositories and shell permissions local
- Provide native project/workspace selection
