# OfferU Web

This is the future React/Vite web console.

Current state:

- The app shell documents the three product layers: job-search product, local agent layer and cloud sync layer.
- The existing `visualizer/` remains the production UI until screens are migrated incrementally.

Run after installing web dependencies:

```bash
npm run web
```
